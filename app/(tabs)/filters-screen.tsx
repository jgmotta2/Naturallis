import { Select } from "@/components/Select";

export default function FiltersScreen() {
  return (
    <Select
      options={[
        {
          label: "pipi",
          value: "toto",
        },
      ]}
    />
  );
}
